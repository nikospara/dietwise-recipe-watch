services:
  recipe-watch-web:
    build:
      context: .
      args:
        INCLUDE_MOBILE_PREVIEW: "false"
    image: recipe-watch-web:local
    container_name: recipe-watch-web
    ports:
      - "8080:80"
    restart: unless-stopped
